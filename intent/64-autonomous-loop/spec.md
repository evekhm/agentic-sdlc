# Spec: the loop merges itself; the human is the escalation path

**Issue:** #64 · **Status:** Approved (approval = merge of this PR) ·
**Author:** athena (`evekhm-athena-app[bot]`) ·
**Open questions:** none

## Amendment r1 (2026-09-08)

The round-3 review raised two high findings (R3-1, R3-2) and one normal finding (AT-6):
- **R3-1** (Argus, high): the `consensus-timeout` escalation path has no trigger. D3's event list is closed, `escalate.sh` has exactly two callers, and the deadline sweep that detects a stale or missing reviewer verdict is explicitly out of scope for #64 because it is #8/#9's work.
- **R3-2** (Argus, high): the two new state-carrying comment types this spec introduces, the escalation marker and the loop ledger, have no author-matching rule.
- **AT-6** (Atlas, normal): the permission-key spelling trap.

**AT-6 is closed as no-action.** D4 already uses the underscore spelling for the App manifest and acceptance 19 the hyphenated spelling for workflow `permissions:`, so the scenario does not apply.

**D3, D8, D9, and D13** are unchanged in text and only qualified by the new decisions below.

| ID | Decision | Rationale |
|----|----------|-----------|
| D21 | **The deadline sweep trigger is #8/#9's work.** The `consensus-timeout` escalation this spec describes has no trigger and no caller until the recorder (#8/#9) lands. No timeout escalation exists today. | Resolves R3-1. A caveat naming #8/#9 as the owner of the sweep trigger. |
| D22 | **Superseded by D23 (Amendment r2).** **Trusted writers.** The trusted writer identities for the escalation marker and for the loop ledger are the merge actor (D4) and `github-actions[bot]`. The identity is checked before either comment is treated as state. | Resolves R3-2. Provides the author-matching rule called for by REVIEW.md's fork-gate policy. |

## Amendment r2 (2026-09-08)

The round-4 review of PR #257 raised two `security` findings, S1 and S2 (comment 5592288923, carried unchanged to head `9d6a39b` by comment 5592295614). Both are gaps in this spec's own design: the implementation at `9d6a39b` is faithful to D22 and to D5 conjunct (2) as written.
- **S1** (Verifier, security): D22 trusts `github-actions[bot]`. On a same-repo `pull_request` event the workflow file runs from the pull request's head, so a pull request can carry a workflow that posts with the default `GITHUB_TOKEN`, and that comment is authored by the trusted login. A forged consensus ledger, loop ledger or escalation marker then flips conjuncts (3), (4), (5) and (11), resets the D13 bounds, or suppresses a real escalation through D9's idempotency test.
- **S2** (Verifier, security): conjunct (2) is implemented by reading `branches/main/protection/required_status_checks`, which needs `administration: read`. D4 grants no such scope, so the read fails on every run and the code substitutes "every check present on the head is success" — a predicate a required job that never reported satisfies vacuously.

Argus's round 1 on this amendment (comment 5592567342, at `495820c`) added one `high` (R1-1, a stall D24 opened) and R1-2 to R1-8; the Verifier's A1 to A3 (comment 5592542563) preceded it. Atlas's pass (comment 5592693368, at `b75d3f4`) added AT-1 (security: P1 did not make the environment the only path to the key) and live evidence that `UNKNOWN` is the normal first read after a push. Every row is resolved in place below and cited where it lands.

**D22 is superseded by D23. D5 conjunct (2) is evaluated as D24 states. D4's grant list is unchanged. D19 carries an in-place r2 note naming what it now admits (R1-3). D9's reason-code set and D10's clearing rule each carry one r2 note for `behind` (R1-1).**

**Preconditions.** Two repository settings close S1 and S2, and this design cannot read either at runtime, because D4 grants no `administration` scope. Each is a named blocking precondition of `loop.autonomous_merge: true`: the pull request that sets the flag true records the discharging evidence for both in `docs/SPEC.md`'s Deployment status, and acceptance 20 is attempted only after it has.

| ID | Precondition | Discharging evidence |
|----|--------------|----------------------|
| P1 | The `merge-actor` GitHub Environment exists, its deployment-branch policy admits `main` and nothing else, and it carries no required-reviewer rule; `MERGE_ACTOR_APP_PRIVATE_KEY` exists only as an *environment secret* of `merge-actor`, and no repository-level or organization-level secret of that name exists, so a job that does not declare `environment: merge-actor` has nothing to read (Atlas AT-1, comment 5592693368). | `gh api repos/<owner>/<repo>/environments/merge-actor` showing `deployment_branch_policy.custom_branch_policies: true` and no `required_reviewers` entry under `protection_rules`, plus `.../environments/merge-actor/deployment-branch-policies` listing exactly `main`; `.../environments/merge-actor/secrets` listing `MERGE_ACTOR_APP_PRIVATE_KEY` and `.../actions/secrets` (repository) and the organization's secrets list not listing it; recorded with the date and the identity that ran it. |
| P2 | Branch protection on `main` names a non-empty required-checks list that includes the repository's CI gate, does not require branches to be up to date before merging (`strict: false`), and grants the merge actor no bypass of required status checks. | `gh api repos/<owner>/<repo>/branches/main/protection/required_status_checks`, run by an admin, showing the `contexts` list and `strict: false`, together with the protection's bypass settings; recorded the same way. |

Both are the operator's to set. Absent P1, D23's trusted login proves nothing and S1 stands; absent P2, `mergeStateStatus` is `CLEAN` or `UNSTABLE` whatever is missing and S2's scenario runs as written. Naming them here keeps an accepted residual risk from becoming an overlooked one (Verifier A3, comment 5592542563).

| ID | Decision | Rationale |
|----|----------|-----------|
| D23 | **One trusted writer, and its credential is unreachable from any pull request's ref.** A comment is state for this design — the loop ledger (D13), the escalation marker (D9), and the recorder's consensus ledger that conjuncts (3), (4), (5) and (11) read — only when its author is the merge actor (D4). `github-actions[bot]` leaves the trusted set; a comment it authored that carries any of those markers is logged by comment id and ignored — it is never state and never a decline. Every write of such a comment is performed with a merge-actor token: `merge_gate.sh` already does; `escalate.sh` does on both of its call paths; and `lifecycle_advance.sh` writes its dispatch rows, its terminal row and its D13/D14 refusal rows under one. The token reaches `.github/workflows/lifecycle.yml` the way it reaches `merge-gate.yml` — minted in the job from `MERGE_ACTOR_APP_PRIVATE_KEY` — and reaches the two scripts by name as `MERGE_ACTOR_TOKEN`; the advancer's `status:*` label writes and its handoff comments stay on `GITHUB_TOKEN`, for the reason this spec's D16 rationale gives: a label written with `GITHUB_TOKEN` starts no workflow (R1-4). **`lifecycle.yml` holds this credential under the discipline D3 gives `merge-gate.yml`: its minting job runs on `push` to `main` and on no other event, checks out `main`'s ref and no other, keeps its `permissions:` block to what it holds today, and passes the minted token to the steps that write ledger rows and call `escalate.sh` and to no other step; its header comment stops asserting that the workflow holds no secret.** Moving these writes onto an App token has the one consequence D16's recursion fact predicts: a comment the App token *creates* fires `issue_comment: created`, which `merge-gate.yml` subscribes to (D3). It converges: the loop ledger is one comment per issue (D13), so only its first row is a creation and every later row is an edit, which that trigger does not fire on; the escalation comment is idempotent on `(reason-code, head-oid)` (D9); and both land on the *issue*, whose comments the gate job skips because it acts only on comments carrying `issue.pull_request` (R1-5). **The property that makes the login a proof of origin is a repository setting, precondition P1: the key lives in the `merge-actor` GitHub Environment, whose deployment-branch policy admits `main` only and which carries no required reviewer, and every job that mints the token declares `environment: merge-actor`.** That setting is unverifiable from inside the loop — D4 grants no scope that reads it — and changing it reopens S1 with no check going red; P1's discharge is a snapshot, and drift in it has no symptom this design can observe (R1-8). P1 also makes the environment the only place the key exists — an environment secret, with no repository or organization secret of the same name — so a job that omits `environment: merge-actor` has nothing to read; the workflow-file grep in acceptance 26 is the tripwire for a workflow that tries (Atlas AT-1, comment 5592693368). A `pull_request` event therefore reaches only the read-only evaluation job — `DRY_RUN=1`, `GITHUB_TOKEN`, a checkout of `main` — which mints nothing; the mutating job runs on D3's other four events: `check_suite`, `status` and `issue_comment` run the workflow file from the default branch, and `workflow_dispatch` runs the file on whichever ref it is given, so for that event the environment policy is the only barrier, and it is sufficient (R1-2). D19 is amended in place for this (its r2 note): the environment and mint step in `lifecycle.yml`, the ledger-write token plumbing in `lifecycle_advance.sh`, and — a gap found while widening — the dispatch and terminal rows D13 already has the advancer write (R1-3). | Resolves S1 (PR #257, comments 5592288923 and 5592295614) and A2 (PR #273, comment 5592542563). A login proves nothing while the credential behind it is available to the code under review, and on a same-repo `pull_request` run both `GITHUB_TOKEN` and every plain repository secret are. "Verify the run behind the comment" was rejected on that ground: no API joins a comment to a workflow run, and a run id embedded in the marker is copied from a real run as easily as the marker is. The environment policy is the one GitHub mechanism that withholds a secret from a non-default ref, which is why D4's "reaches the workflow by name" becomes a statement about *which runs* may hold it. Ignoring an untrusted marker is deliberate where a decline was available: a decline would hand a veto to anyone who can comment on the issue. Naming the recorder's ledger is owed because the gate already author-checks it and S1's failure scenario forges that one first; a constraint on what this gate accepts is the whole of what this design may say about #8/#9. The `lifecycle.yml` invariant is owed because today's containment is an accident of that file's single trigger: a later round adding a second one for a sensible reason would hand the minting job to pull-request code while violating no line of this spec (A2). |
| D24 | **Conjunct (2) is GitHub's own merge-state verdict, read through a pull-request scope; the gate never reconstructs it from a required-checks list.** (2) is true when all three hold: the pull request's `mergeStateStatus` is `CLEAN` or `UNSTABLE`; every check run and commit status on the head OID other than the gate's own run is `success`; and at least one such check exists. **The gate's own run is identified by identity and never by name: the one roll-up entry whose `checkSuite.workflowRun.databaseId` equals the evaluating job's `GITHUB_RUN_ID`. At most one entry is excluded, and every other entry counts whatever its name, `merge-gate` included.** `BLOCKED`, `DIRTY`, `DRAFT` and `HAS_HOOKS` make (2) false; **`BEHIND` makes (2) false and, because nothing in this design updates a branch, is also the one false conjunct that escalates: the gate calls `escalate.sh` with `reason-code: behind`, a seventh code added to D9's closed set, idempotent on `(behind, head-oid)` like the others, and D10 treats a `behind` marker as live until an evaluation finds the head no longer `BEHIND`.** P2 records that `main` does not require branches to be up to date, so `BEHIND` is unreachable while P2 holds, and an observed `BEHIND` is P2 drift made visible; **On `UNKNOWN` the gate re-reads the field up to three more times at two-second intervals before deciding**, because GitHub computes it lazily: a read immediately after a push returns `UNKNOWN` and a read seconds later returns the verdict (Atlas, comment 5592693368). `UNKNOWN` on the last read, an unreadable field, or an unreadable check roll-up is unevaluable: no merge, no escalation, the ledger row names `UNKNOWN` as the cause, and the next D3 event re-evaluates — `check_suite` and `status` are the events that follow a push, and they arrive after the field is computed, so a run that stays `UNKNOWN` is a stall the ledger shows. `merge_gate.sh` makes no call under `branches/*/protection`; D4's grant list is unchanged and gains no `administration` scope; and the merge actor is granted no bypass of required status checks, so `PUT .../merge` refuses whatever the gate misjudged. The required-checks setting on `main` is precondition P2 and is what turns a skipped required job into `BLOCKED`. | Resolves S2 (PR #257, comments 5592288923 and 5592295614) and A1 (PR #273, comment 5592542563) and Argus R1-1 (PR #273, comment 5592567342). Of the two routes offered, `administration: read` was rejected as a repo-admin-adjacent scope on the one identity that can write to `main`, held so the gate can copy a setting GitHub already enforces; and "fail closed on an unreadable list" alone was rejected because under D4's grants the list is unreadable on every run, so that rule would have merged nothing, ever — a bricked gate carrying fail-closed's name. `mergeStateStatus` is the direct read: it is computed by the rule the merge endpoint enforces, it is `BLOCKED` when a required context has not reported — the case a roll-up of present checks cannot see — and it is readable under D4's existing grants: GitHub documents the field as needing push access, which `contents: write` carries, and one GraphQL read with the minted merge-actor token returned `CLEAN` for PR #257 on 2026-09-08 (R1-7). `UNSTABLE` is admitted alongside `CLEAN` because the gate's own in-progress evaluation run on the head is a non-passing, non-required status; the second and third clauses keep the existing stricter reading as conjuncts, so a pending or failed non-required check still declines. Exclusion by identity is owed because a check run's name is its job's `name:`, which the pull request under evaluation sets by adding a workflow file: a foreign failing job named `merge-gate` would be dropped as the gate's own while `UNSTABLE` admits it, and conjunct (2) would come out true with a red check on the head — S2 by a second route, load-bearing now that the required-contexts read is gone (A1). The run id is the one fact about its own check a job holds that no other workflow can claim, and admitting `UNSTABLE` is safe only because clause 2 counts every foreign entry. A gate that mirrors GitHub's verdict and leaves enforcement to the merge endpoint is #25 D2's one-source rule applied to a repository setting. `BEHIND` escalates because a plain decline on it is a resting state with no exit: both verdicts recorded, blocking set empty, no D9 cause, no D8 clock, and no automatic evaluation that can ever clear it, since nothing here rebases (R1-1). Recording the setting in P2 alone was rejected because a setting the gate cannot read can drift, and an escalation is the one symptom of that drift this design can produce — which is also why `behind` clears on a head that is no longer behind and never by the passage of time: the operator is told each time the setting bites, until the setting is fixed. Requiring up-to-date branches was not adopted because every merge would invalidate every other open head, each rebase invalidates the consensus that authorised it (conjunct 3), and the loop would pay two review rounds per unrelated merge. |
| D25 | **`MERGE_ACTOR_TOKEN` reaches `lifecycle.yml` as the env of the one step that runs `scripts/ci/lifecycle_advance.sh`, and of no other step; inside the script only the ledger-row write and the `escalate.sh` call read it, and every other `gh` call keeps `GH_TOKEN` as `github.token`.** Acceptance 26's "only the ledger-writing and escalate.sh steps receive it" names that step, because the advancer's label writes and ledger rows are interleaved per issue and the advancer step *is* the ledger-writing step. **[r3 (D30): one further call in the script carries `MERGE_ACTOR_TOKEN` — the `viewer { login }` read that resolves the trusted writer's own login — because that login read on `github.token` would name the writer D23 excludes; acceptance 26's step-level rule is unchanged.]** | Builder question 1 (PR #257, Fable, 2026-09-08). Splitting the ledger writes into their own step would restructure the advancer past what D19 admits, for no gain: the token's exposure is the step's env either way, and the script-level rule is what acceptance 26's grep checks. |
| D26 | **`escalate.sh` performs its comment write and its marker read under `MERGE_ACTOR_TOKEN`, and its `status:*` → `status:review-stuck` label swap under the caller's `GH_TOKEN`, on both call paths.** In the gate job both are the merge actor's token; in `lifecycle.yml` the swap stays on `github.token`, which starts no workflow. | Builder question 2. The rule is D16's: label writes stay on the token that wakes nothing, comment writes go where D23 puts them. Running the whole script under the App token would make the swap fire a `labeled` event from the advancer path for no benefit. |
| D27 | **D18 governs `behind` as it governs every other escalation: with `loop.autonomous_merge: false`, `BEHIND` is a plain decline recorded in the ledger row and escalates nothing.** | Builder question 3. P2 is a precondition of autonomous merging and is discharged by the pull request that sets the flag true (acceptance 28); before that, `main`'s up-to-date setting is unconstrained and a `behind` escalation would report drift from a setting nobody has promised yet. D18's text — "no merge, no escalation and no D10 restore" — already says this; the row makes it explicit so round 3's choice stands unchanged. |
| D28 | **A trusted `behind` marker clears at the first evaluation whose current head has a `mergeStateStatus` other than `BEHIND` and other than `UNKNOWN`, whatever head-oid the marker carries.** `UNKNOWN` after D24's bounded re-read, or an unreadable status, is unevaluable and clears nothing. | Builder question 4. D10's current-head rule is right for markers about a head's own defects; a `behind` marker is about the head's relation to `main`, and the head that is no longer behind is by construction a different one, so head-oid equality would leave every `behind` marker uncleared forever. |
| D29 | **Acceptance 27's recorded `mergeStateStatus` read is owed by the pull request acceptance 28 names — the one that sets `loop.autonomous_merge: true` — and PR #257 records no live read.** The 2026-09-08 read (`CLEAN` for PR #257 with the minted merge-actor token) is the current evidence and stands until that pull request replaces it. **The `pull_request` `evaluate` job's own check-run must conclude `success` on a decline in `DRY_RUN`: it is a foreign entry to every later gate job's clause 2, and a red one would decline every head forever.** | Builder question 5 and the builder's two stated intentions. The App token is minted only inside a workflow job under the `merge-actor` environment, so a builder cannot record the read from a pull request that has not merged; the flag-flipping pull request is the one whose evidence acceptance 28 already collects. The roll-up read uses `gh api graphql` (`checkSuite { workflowRun { databaseId } }` for check runs, `context`/`state` for statuses) because `gh pr view --json statusCheckRollup` does not expose the run id; `UNKNOWN` under the `evaluate` job's read-only token is the unevaluable path and a decline in `DRY_RUN`, which the pull request body notes. |

## Amendment r3 (2026-09-09)

The round-5 verification of PR #257 (Argus comment 5593511056, Atlas comment 5593510358, both at `87c0da0`; Argus re-confirmed at `a619de3` in comment 5595602611) carried three rows on one gap in this spec:
- **R4-2** (both reviewers, in-round `high`): D23 makes the merge actor's own login the one test a state-carrying comment must pass, and names no way for the code to learn that login. Round 4's S3 (comment 5592288923) asked that it be derived from the token in scope and suggested `GET /user`; `merge_gate.sh` and `escalate.sh` followed the suggestion. Each reviewer reproduced live, under a real App installation token, that `GET /user` answers HTTP 403 `Resource not accessible by integration` for that credential — it answers only for a user's own token — so the gate declined at its first line and the escalator refused, on every invocation, and the suite stayed green only because its fixture made the call succeed. The repository already records the platform fact three files away: `scripts/ops/post.sh:159-165` says an installation token is not a user and `GET /user` is 403 for it.
- **R4-4** (`normal`): `lifecycle_advance.sh` still read its trusted writer from `MERGE_ACTOR_LOGIN` with a hardcoded default — the guess S3 removed from the other two scripts.
- **R5-1** (`normal`): `docs/SPEC.md` stated the 403ing mechanism in the present tense.

The evidence was re-run for this amendment on 2026-09-09 under the `evekhm-odyssey-app` installation token, minted by `scripts/auth/mint_app_token.py`: `gh api user` → 403; `gh api graphql -f query='{viewer{login}}'` → `evekhm-odyssey-app[bot]`. Written by the implementer under the frontier-tier dispatch on PR #257, on the reviewers' evidence; as this file's header says, the merge is the acceptance and an edited row is the decision.

**D23 and D24 are unchanged. D25 carries an in-place r3 note. D30 is the one new row.**

| ID | Decision | Rationale |
|----|----------|-----------|
| D30 | **The merge actor learns its own login from the credential it holds, through one GraphQL read — `viewer { login }` — made with the merge-actor token, and from nothing else: never `GET /user`, never an environment variable, never a constant.** All three scripts resolve it this way: `merge_gate.sh` and `escalate.sh` at start, under the token they write with; `lifecycle_advance.sh` under `MERGE_ACTOR_TOKEN`, once, the first time it reads a loop ledger. A failed or empty read is unevaluable and falls back to no name: `merge_gate.sh` declines, `escalate.sh` refuses with nothing written, and `lifecycle_advance.sh` treats the ledger as unreadable — "unreadable is not absent" (D13) — so the label is not written and the run is red. A read that answers `github-actions[bot]` is the same refusal: D23 removes that login from the trusted set, and a process that finds itself under it is a process that does not hold the merge actor's credential, whatever put it there. The `pull_request` `evaluate` job, which holds no merge-actor token (D23), therefore reaches this refusal on every run, and D29's rule — a decline in `DRY_RUN` concludes `success` — is what keeps that expected outcome green. | Resolves R4-2, R4-4 and R5-1 (PR #257, comments 5593511056 and 5593510358). `viewer` is answered for an installation token — three live reads under three different Apps on this thread — and it answers the exact question D23 asks, "who holds this token", in the exact form GitHub uses for a comment author's `login` (`<slug>[bot]`), which is the string `trusted_only` compares. The other route, `actions/create-github-app-token`'s `app-slug` output, was rejected on three grounds: it yields the slug without `[bot]`, so the login is rebuilt by convention — a guess again, one suffix away from S3; it has to be plumbed through an env var into every caller, which restores the env-var surface S3 removed and leaves a local run, and any caller that forgets the plumbing, with nothing; and it names the App the key was for, where `viewer` names the holder of the token actually in scope, which is the only fact the trusted-writer test may rest on. `GET /app` was rejected for the reason `post.sh` gives: it answers about the App the JWT signed for and needs that JWT, a second minting path. Resolving on `github.token` was rejected because its answer is the one login D23 excludes; that is why the advancer's read is the one further call D25 now admits, and why an answer of `github-actions[bot]` is a refusal. Fail-closed keeps S3's shape: an unresolved identity trusts nobody, so a forged ledger under any name is prose, and the cost of the failure mode is a stalled loop the ledger and the red run show; a wrong merge is unreachable from it. |

## Amendment r4 (2026-09-09)

The operator named the App that holds the merge-actor role (PR #257 thread, 2026-09-09): it is **Themis**, slug `<owner>-themis-app` (login `evekhm-themis-app[bot]` on this repository), registered from the `themis` entry of `scripts/auth/app_manifests.yaml`, which carries `kind: system`. The role noun "merge actor" in D4, D13, D23, D25, D26 and D30 is unchanged, and so are the script-level names `MERGE_ACTOR_TOKEN` and `MERGE_ACTOR_LOGIN`. Three identity-bearing names change wherever this spec states them:

| This spec says | Read as |
|----------------|---------|
| the `merge-actor` GitHub Environment; `environment: merge-actor` | the `themis` GitHub Environment; `environment: themis` |
| `MERGE_ACTOR_APP_PRIVATE_KEY` | `THEMIS_APP_PRIVATE_KEY` |
| `MERGE_ACTOR_APP_ID` (the mint step PR #257 adds) | `THEMIS_APP_ID` |

**D4, D23, D24, D25 and D30 are unchanged in substance; D23's environment name and D25's secret name read as the table says. D31 is the one new row.**

| ID | Decision | Rationale |
|----|----------|-----------|
| D31 | **Themis is a system actor, and the tooling that registers the six personas registers and provisions it.** `scripts/auth/create_all_apps.py` iterates every entry of `app_manifests.yaml` in file order. An entry with `kind: system` is registered through the manifest flow and installed on this repository like a persona; it is then provisioned: the Environment the entry names, a deployment-branch policy that admits `main` and no other ref, and the two Environment secrets the entry names, loaded from the key the registration saved. Provisioning is idempotent and re-runnable. `create_all_apps.py --check` reports every entry's state read-only, and its output is the evidence acceptance 28's pull request pastes for P1. Themis has no `personas/<name>.yaml`, no prompt, no harness binding and no row in `config/execution.yaml`; a claim, a review or a pull request authored under its login is a defect. | The operator found on 2026-09-09 that PR #257 shipped the manifest entry while the wrapper hardcoded the six persona names and read `personas/<name>.yaml` for each, so a from-scratch install produced six Apps and a lifecycle workflow that fails at its mint step, and that no tooling created the Environment P1 rests on. The operator asked for a name in the cast's register; the role noun stays so that D4's grant list and D23's trust rule still read as statements about a role, whoever holds it. |

## Amendment r5 (2026-09-09)

Issue #288 tracks three text updates to align this specification with the autonomous dispatch architecture landed across PR #257, PR #294, and PR #297:
- **Placement adapter argument contract**: D16 originally specified that `lifecycle_advance.sh` invokes the placement adapter "with the issue number and nothing else". Both `scripts/placement/vm-local/run.sh` and `scripts/placement/gh-actions/run.sh` enforce `<number> --as <persona>`, exiting with an error if `--as <persona>` is missing. In `scripts/ci/lifecycle_advance.sh:1159`, the advancer passes `"$issue" --as "$persona"`. Amendment r5 authorizes and requires passing `--as "$persona"`, resolving the specification discrepancy tracked under defect #284.
- **VM poller dispatch row consumption and record-and-return**: D16 originally described the advancer as the sole dispatcher. Under the hybrid placement architecture established in #251 (D2, D5), builder personas run on the operator VM. When executed inside GitHub Actions, `scripts/placement/vm-local/run.sh` executes record-and-return (logs delegation and exits 0), and an external VM poller daemon (`scripts/placement/vm-local/poll.sh`) consumes unconsumed `dispatch` ledger rows on the operator VM. `.github/workflows/lifecycle.yml` retains its triggers (`push` to `main` only) and permissions block (no `actions: write`), preserving D23, D25, and D26.
- **D19 scope boundary clarification**: D19 listed touched and prohibited files for "The implementing change" of issue #64 (PR #257). Amendment r5 adds the clarification that D19 defines the scope boundary solely for issue #64's own implementing pull request and imposes no restriction on other issues (such as issue #251 modifying `scripts/ops/work.sh` under #251 D8).

**D16 gains in-place revision notes for D32 and D33. D19 gains an in-place revision note for D34. Acceptance item 16 is qualified by acceptance 29. D32, D33, and D34 are the three new rows.**

| ID | Decision | Rationale |
|---|---|---|
| D32 | **Placement adapter `--as "$persona"` invocation argument.** `lifecycle_advance.sh`, immediately after writing the `status:*` label and loop-ledger dispatch row, invokes `scripts/placement/<placement>/run.sh "$issue" --as "$persona"`. Passing `--as "$persona"` is authorized and required across placement adapters (`vm-local`, `gh-actions`), matching each adapter's CLI contract (`<number> --as <persona>`). D16's original phrase "with the issue number and nothing else" is superseded by this requirement. This resolves the specification defect behind #284. | Resolves issue #288 item 1 and defect #284. Both placement adapters check for `--as <persona>` and fail closed with a usage error if it is missing. Passing the target persona explicitly enables adapters to determine execution ceiling, harness bindings, and persona identities deterministically. |
| D33 | **VM poller dispatch row consumption and record-and-return.** For `vm-local` placements, `lifecycle_advance.sh` appends the D13/D16 `dispatch` row to the loop ledger and invokes `scripts/placement/vm-local/run.sh "$issue" --as "$persona"`. When executed inside GitHub Actions (`GITHUB_ACTIONS=true`), `scripts/placement/vm-local/run.sh` performs record-and-return: it logs delegation to the operator VM poller and exits 0. The external VM poller daemon (`scripts/placement/vm-local/poll.sh`, specified in #251 D2 and D5) consumes unconsumed `dispatch` ledger rows on the operator VM, acquires the issue claim, and executes the local launch. The loop ledger remains append-only and immutable; Themis remains the single trusted writer of loop ledger comments (D23). `.github/workflows/lifecycle.yml` retains its triggers (`push: branches: [main]` only) and permissions block (no `actions: write`), preserving D23, D25, and D26 token boundaries. | Resolves issue #288 item 2 and #251 D2/D5 follow-up. Builders require private keys and execution environments that are hosted on the operator VM to prevent exposing builder credentials to pull request workflows. Record-and-return allows GitHub Actions to record the authoritative loop ledger transition without requiring runner-to-VM direct communication or elevated workflow permissions. |
| D34 | **D19 scope boundary clarification for subsequent issues.** D19's file list and prohibitions define the scope boundary solely for issue #64's own implementing pull request (PR #257). D19 imposes no prohibition or constraint on other issues (including #251 modifying `scripts/ops/work.sh` under #251 D8). | Resolves issue #288 item 3 and #251 D8 follow-up. Each issue defines its own scope boundary in its specification. A scope list written to bound one PR cannot prevent subsequent issues from evolving repository tooling under their own accepted designs. |

The accepted intent (`intent.md`, merged in 0b15da4) carried five Open
questions, a per-issue ceiling, and a monotonic-progress invariant to
this rung. No human was live in the design session, so each is
resolved below with its rationale grounded in the record rather than
posed back. **Any row in this table can be overruled by editing this
file at the merge gate** — the merge is the acceptance, so an edited
row *is* the decision.

Open question 1 lands as D1 and D2; question 2 as D3 and D4; question
3 as D7 and D8; question 4 as D9, D10 and D11; question 5 as D12. The
permanent merge policy the operator's 2026-09-04 interim starts from
is D5 and D6; the per-issue bound and the monotonic invariant are D13
and D14; `hold` is D15; self-dispatch is D16; and the one amendment
this design owes an earlier spec — `config/execution.yaml`'s schema —
is D20.

The interim policy in force since 2026-09-04 — merge when CI is green
and both reviewers' latest rounds carry an empty blocking set at the
current head, escalate otherwise — is exactly conjuncts (2), (3) and
(4) of D5 and nothing else. Everything below adds the eight conjuncts
an unattended writer needs that a human performing the same check
supplies from judgement.

## What is being built

```text
scripts/ci/merge_gate.sh              new: the deterministic merge writer
scripts/ci/escalate.sh                new: the ONE escalation writer (D9)
scripts/ci/tests/merge_gate_test.sh   new: hermetic scenarios, stub gh
.github/workflows/merge-gate.yml      new: events + merge-actor App token
.github/workflows/lifecycle.yml       + permissions, the config read, one escalation step
scripts/ci/lifecycle_advance.sh       + the next-rung dispatch step (D16)
personas/lifecycle.json               unchanged (D17)
config/execution.yaml                 + the loop: block; three trigger flips (D20)
scripts/ops/execution.py              + the loop: schema, the `ladder` trigger, --loop (D20)
scripts/auth/app_manifests.yaml       + the merge actor (not a persona)
docs/SPEC.md                          loop.autonomous + lifecycle.labels + D20
AGENTS.md, INTENT.md, README.md, REVIEW.md   the four false-text repairs
```

**The base, as of round 3.** `config/execution.yaml` and its single
parser `scripts/ops/execution.py` are on `main`: #25 delivered them,
and the `execution` job of `.github/workflows/ci-gates.yml` already
runs `python3 scripts/ops/execution.py --check` over that file on
every pull request. What this issue adds to the pair is D20's `loop:`
block and its `ladder` trigger value, and nothing else. The
**recorder** (#8/#9) still does not exist on `main`, so D5's conjuncts
(3), (4), (5) and (11) have no ledger to read until it does.

## Decisions

| ID | Decision | Rationale |
|----|----------|-----------|
| D1 | **One policy for all five rungs: every rung's pull request is reviewed under REVIEW.md unchanged and merged by the same predicate (D5). There is no prose path and no code path.** Reading (b) of Open question 1 — prose merges on a structural check with no model verdict — is rejected as the *whole* gate and adopted as one conjunct of D5 (D2). | (a) is already what this repository does: PR #94 (an `intent.md`) and PR #96 (a `spec.md`) both drew full Argus and Atlas rounds with severities, IDs and ledgers, and REVIEW.md already provisions the case — "Where the change has no Decisions table (documentation changes, bootstrap PRs …), findings key to their own IDs and everything else in this protocol is unchanged" (REVIEW.md:317-320). The differing case the intent names — a well-formed, `Approved`, internally contradictory `spec.md` — is exactly what PR #96's round 1 caught (R1-1 found D9's premise false against D15 in the same file), so reading (b) would have shipped that contradiction to the build rung. The cost objection is real and is answered by the funnel rather than by a second policy: rounds 2 and 3 are verification-scoped (REVIEW.md:143-150), so a clean prose artifact costs two round-1 runs, not a loop. The stall objection — a reviewer blocks prose wrongly — is answered by D9: the round cap escalates, it does not wait. |
| D2 | **Severity for a prose artifact is the same closed `high` list (REVIEW.md:102-111), read through to the capability the artifact specifies: a finding is `high` if and only if the artifact **as written** would, when built, produce one of those five outcomes. No tier is added, no tier is redefined, and the `failure_scenario` field names the built consequence, not the sentence.** Separately and *not* as a finding, three structural facts are conjunct (9) of D5, checked deterministically by the merge gate: the rung's artifact exists at `intent/<n>-<slug>/<artifact>` for the row's `artifact` in `personas/lifecycle.json`; a `spec.md` carries `Status: Approved`; and a `spec.md`'s `Open questions` line reads `none`. **A row whose `artifact` is `null` — `implement` and `review`, two of the five rows — satisfies the existence fact *vacuously*: there is no file to look for, the fact is TRUE, and D5's fail-closed rule for an unevaluable conjunct is never reached.** | Extending the closed list would be a REVIEW.md amendment this issue did not ask for, and the list's whole purpose is that it is closed (REVIEW.md:56-61: "Blocking is a closed definition, not reviewer judgment"). Read-through needs no amendment because every prose rung's output is a specification *of* the capability the list already describes: "an unbounded loop, redispatch, or self-trigger" is the tier R1-1 correctly gave an intent that lacked a bound, on PR #94. The structural facts are conjuncts rather than findings because they are decidable by `grep` and a model verdict on them would be a model deciding a merge, which the intent forbids ("a model must not be the thing that decides a merge"). They also move a check that exists today from *after* the merge to *before* it: `lifecycle_advance.sh`'s Draft override currently posts a warning and refuses to advance a merged non-Approved `spec.md` (`docs/SPEC.md`, `lifecycle.labels`), which under an autonomous loop would be a merged artifact and a stopped ladder. The null case is stated rather than left to the implementer because it is the *implement* rung — the one whose pull request carries the code — and the two available readings are not close: read as a satisfied fact it merges, read as a gap D5 fail-closes and no implementing pull request can ever merge, forever (Argus R2-9). |
| D3 | **The merge write is performed by `scripts/ci/merge_gate.sh`: deterministic bash + `gh` + `jq`, no model call, `DRY_RUN=1` printing every mutation instead of executing it, runnable locally by the same command CI runs.** It reads recorded state (the ledger, the labels, the check runs, the loop ledger) and writes at most one merge, one loop-ledger row, and — on a refusal that escalates — one escalation, which it does not perform itself but delegates to `scripts/ci/escalate.sh` (D9), the sole writer of `status:review-stuck` and of D9's marker comment anywhere in this design. **There is no escalation-drain mode**: nothing queues an escalation for a later run to notice (D14). It never files a finding, never edits a ledger row's verdict, and never resolves a disagreement (D6). **`.github/workflows/merge-gate.yml` runs on exactly the events that can change a conjunct of D5 — `pull_request`, `check_suite: completed`, `status`, `issue_comment: created` and `workflow_dispatch` — and on no others: never `push`, never `pull_request_target`.** Those runs are also "each evaluation" as D10 uses the phrase. | The intent's determinism constraint and `lifecycle.labels`'s existing shape are the same shape, and the predecessor already computed this predicate in bash and jq. Reading (b) of Open question 2 — branch protection plus GitHub auto-merge — is rejected on the two charges the intent already prices: a required *approving review* would end the reviewers' comment-only authority (REVIEW.md:36-39, README:112), which is a policy reversal this issue did not ask for; and auto-merge re-evaluates checks, not labels, so a `hold` applied at 14:00 to a pull request whose checks went green at 13:59 does not stop it. A script that re-reads immediately before the write does (D15). The trigger list is named here rather than left to the implementer because D10's self-clearing escalations are defined "on each evaluation", and a set of evaluations no Decision states is a set no test can enumerate. `push` is excluded deliberately: round 2 added a `push: main` trigger for the sole purpose of draining refusal rows, and D14 no longer produces a queue for anything to drain (Atlas AT-4, Argus R2-1). |
| D4 | **The merge actor is a GitHub App installation distinct from all six personas and distinct from `GITHUB_TOKEN`, holding `contents: write`, `pull_requests: write`, `issues: write`, `checks: read` and `statuses: read` on this repository and nothing else — the three writes the merge, the loop ledger and the escalation need, and the two reads D5 conjunct (2) cannot be evaluated without. It has no persona source, no prompt, no model and no `personas/lifecycle.json` row — it is infrastructure, like `github-actions[bot]`.** Its key reaches the workflow by name (`MERGE_ACTOR_APP_PRIVATE_KEY`), never in argv and never in a prompt. Its entry lands in `scripts/auth/app_manifests.yaml`; `personas/**` is untouched. | Three things fall out at once. "No agent merges its own work" holds *by construction* rather than by a check, because the merge actor is never the author of anything (intent, Constraints). Least privilege is exactly the writes this design performs plus the reads conjunct (2) requires, and round 2's list was short by one: the merge endpoint is a pull-request endpoint that writes a commit, so it needs `contents: write` and `pull_requests: write` together — but D9's escalation (a label swap and a comment on the **issue**) and D13's loop ledger (a comment on the **issue**) are *issue* writes, which neither of those scopes covers, so every escalation and every ledger row would have 403'd (Atlas AT-5, Argus R2-3). `.github/workflows/lifecycle.yml`'s own header already holds exactly `issues: write` for those same two operations, which is the confirmation rather than the guess. Check runs and commit statuses are readable under neither `contents` nor `pull_requests`, and a gate that cannot read them fails closed on every pull request. All five sit on an identity that has no other duty. And the recursion fact decides it against the cheaper option: **an event generated by `GITHUB_TOKEN` does not start another workflow run**, so a merge performed with the default token would push to `main` and `.github/workflows/lifecycle.yml` would never run — the loop would merge and then stop dead at the label, which is the stall this issue exists to remove, reintroduced one layer down. An App installation token has no such suppression. Reading (a) of Open question 2 asked whether the merger is "a merger persona or an infrastructure identity"; it is the second, which is why `personas/**` and INTENT.md's persona cast do not change (#25, D10). |
| D5 | **`merge-eligible` is a conjunction of eleven facts, all read from recorded state, all re-read immediately before the write. Any false conjunct means no merge; a conjunct that cannot be evaluated means no merge (fail-closed).** (1) The base is the default branch and `head.repo.full_name` equals this repository — no fork (#25, D7; REVIEW.md:420-425). (2) Every required check run and every required commit status for the head OID is `success`; a pending, failed or missing required check is false, not "not yet" **[r2: how (2) is evaluated is D24]**. (3) Both reviewers' verdicts cover the current head OID: Argus's must be recorded against `Reviewed-head: <full-oid>` equal to the current head, and Atlas's either is recorded against that OID or carries forward to it under D7's carry-forward rule. (4) The blocking set — open `security` plus open `high` rows — is empty (REVIEW.md:331-332). (5) The consensus axis is `agreed` and no blocking row carries a `dispute` verdict (REVIEW.md:335-339, D6). (6) Neither `hold` nor `blocked` is present on the pull request or on the issue it belongs to or closes (#25, D14; D15). (7) The merging identity is not the pull request's author (D4). (8) The pull request's issue is under both per-issue bounds (D13). (9) The rung's structural facts hold (D2). (10) The rung index is strictly greater than the highest rung index the loop ledger records as merged for that issue (D14). (11) The ledger carries a head marker and the head probe succeeded (REVIEW.md:346-348). | Conjuncts (2), (3) and (4) are the operator's 2026-09-04 interim policy restated mechanically — "CI is green" is (2), "both reviewers' latest rounds at the current head" is (3), and "an empty blocking set" is (4). The other eight are this spec's additions, conjunct (5) among them: the interim policy checks no consensus/dispute axis at all. The remaining seven are the judgements a human supplied silently and an unattended writer must be given. Each is a fact already recorded by something that exists or is specified: (1) by the API payload, (2) by GitHub's own check API — REVIEW.md's `merge-ready` "is silent about CI, which is GitHub's own signal" (REVIEW.md:342), so CI is added *here* rather than by amending that definition, (3)–(5) and (11) by the recorder's ledger, (6) by the labels, (8) and (10) by the loop ledger (D13). Fail-closed on an unevaluable conjunct is the same rule `spec_check.sh` already applies to its own inputs and is what stops a gate from "running, deciding nothing, and reporting success" — itself an item on the closed `high` list (REVIEW.md:106-108). |
| D6 | **Disagreement is a recorded fact, never an inference: the pull request is in disagreement when the ledger carries a `dispute` verdict on any blocking row, or when the two reviewers' latest verdicts on the same Decision ID differ. The merge gate never adjudicates one — it refuses to merge and lets the funnel run; at the round cap it escalates (D9).** A disagreement over a non-blocking row is recorded and does not affect eligibility. | REVIEW.md already fires `disputed` "on an explicit dispute over ANY blocking row, because a contested high finding must stay visible even though it needs no routine peer verdict" (REVIEW.md:337-339), and already routes an unresolvable one to a human at three exchanges with both positions summarised (REVIEW.md:283-285). Both are consumed unchanged. Keying the second clause to Decision IDs rather than to diff hunks is REVIEW.md's own reconciliation rule (REVIEW.md:302-305), and it is what makes "the reviewers disagree" decidable by a script instead of by a reader. A merger that adjudicated would be a third reviewer with no protocol, and one whose verdict is a merge. |
| D7 | **Consensus that authorises a merge requires both reviewers to have posted a verdict against the current head OID. A missing verdict is not agreement.** A reviewer that dies, times out, abstains, or never starts blocks the merge; the item then reaches D8's deadline and escalates. `security` rows keep both reviewers' explicit AGREE, twice, unrelaxed (REVIEW.md:257-259). **Carry-forward, and only for Atlas: Atlas's most recent recorded verdict covers a later head when all five hold at that head — (i) Atlas posted a full round-1 verdict on this pull request at an earlier head; (ii) no `AT-*` blocking row is open; (iii) no `security` row is open, and none was fixed after Atlas's last verdict; (iv) no dispute and no human mention naming Atlas is outstanding; (v) no deep-review grant is outstanding at this head — no admin-applied `deep-review` label that has not been consumed, no deep manual dispatch of the review workflow that no verdict at this head has answered, and no explicit human request or mention in the thread for a deep or full review that no verdict at this head has answered (REVIEW.md:372-387). If any of (i)–(v) fails, only a verdict recorded at the current head OID satisfies conjunct (3). Carry-forward never applies to Argus, who reviews every round.** | Reading (b) — merge on one clean verdict plus no objection by a deadline — converts the dual-family design into a single-reviewer loop *precisely* when the second family is broken, which is the one circumstance the second family exists for. REVIEW.md's "where nothing is in scope, require no consensus" (REVIEW.md:352-354) is a rule about **rows**, not about reviewers: it says an item with no security rows needs no verdict on nothing, not that an item with no *reviewer* needs no review. The predecessor's "nine pull requests agreed and nothing could merge" is not reproduced under (a), because unlike the predecessor this resting state has a reachable exit — D8's deadline escalates it, which is exactly the fix REVIEW.md:352-355 prescribes. Conjunct (3) without carry-forward would escalate the ordinary pull request rather than the exceptional one: REVIEW.md:260-263 has Atlas run "round 1 in full; afterwards only to verify its own open blocking `AT-*` rows, to co-sign a security fix, or to answer an explicit dispute or mention. It does not re-review deltas containing neither" — so a clean Atlas round 1 followed by an Argus-only fix round leaves Atlas correctly silent at the new head forever, and a literal reading of (3) would hand every one-sided pull request to a human as `consensus-timeout`. That is the common path, not an edge case, and it is the stall this issue exists to remove. The other reading — require Atlas to post a one-line verdict at every head — is rejected because it amends REVIEW.md's cadence and its costed envelope ("Worst case per PR: three Argus review runs, one full Atlas run", REVIEW.md:429-430) for every pull request in the repository, which this issue did not ask for. Conditions (i)–(iv) are not new policy: they are the exact complement of REVIEW.md's own list of what brings Atlas back, so the gate carries a verdict forward in precisely the cases where REVIEW.md says Atlas has nothing to add. Condition (v) is owed because (i)–(iv) complement REVIEW.md's **cadence** and not its escape hatch: the deep-review grant exists because "the funnel bounds the automatic loop; it must never bound the human" (REVIEW.md:373), an admin's label is "consumed on use" (REVIEW.md:378-380), and a request outranking Atlas's cadence "gets a round-1-depth pass at the stated head" (REVIEW.md:384-386). Without (v), a gate could satisfy an explicitly purchased full-depth round with a verdict recorded at an older head and spend the grant on a review nobody ran (Argus R2-8). |
| D8 | **The stale-peer deadline escalates; it never authorises. When a pull request has one reviewer's verdict at the current head and the peer's row has been pending past the deadline, the item escalates (D9) instead of merging.** The clock is the ledger row's own pending timestamp (REVIEW.md:213-214, 220-222) and the check is the existing zero-token deterministic sweep (REVIEW.md:405-409) — no new clock is introduced. An explicit human request for the missing review (mention or manual dispatch, REVIEW.md:403-404) **resets** that row's deadline exactly once per head OID; a second request at the same head does not reset it again. A row that carries forward under D7 is not pending and starts no deadline clock; the deadline applies only to a reviewer whose verdict conjunct (3) still requires at the current head. | This is the whole content of the sub-question, and it is a question about *authority*, not about time: the clock already exists and already alerts, and reading (b) would grant it a new power — to authorise an irreversible write — that nothing in the record gave it. Reset rather than bypass, because an operator who asks for the missing review one minute before the deadline and then watches it merge without that review got the opposite of what they asked for; once per head rather than unbounded, because an unbounded reset is a human accidentally disarming the escalation by repeating a mention. Extend was rejected as reset-with-extra-arithmetic. |
| D9 | **An escalation is exactly two writes on the issue: the single `status:*` label is **swapped** for `status:review-stuck`, and one comment is posted stating both positions in two lines and naming the decision being asked for. Nothing else stops, nothing else is written.** The swap is one `gh issue edit` so the "at most one `status:*`" invariant is never transiently violated (`docs/SPEC.md`, `lifecycle.labels`). The comment carries a machine marker `<!-- escalation:<displaced-label>:<reason-code>:<head-oid> -->` where `<reason-code>` is one of `consensus-timeout`, `blocking-open-at-cap`, `dispute-at-cap`, `security-open`, `budget`, `non-monotonic` **[r2: plus `behind` — D24]**. **Both writes are performed by one script and by nothing else: `scripts/ci/escalate.sh <issue> --reason <reason-code> --head <oid> [--pr <n>]`. It has exactly two callers — `scripts/ci/merge_gate.sh` (D3) and `.github/workflows/lifecycle.yml` on a D14 refusal (D14) — and one format between them.** **An escalation is idempotent on `(reason-code, head-oid)`: before writing anything, `escalate.sh` reads the issue's comments to exhaustive depth (D13's pagination rule) and, if a marker carrying the same `<reason-code>` and the same `<head-oid>` is already present, it writes no label, posts no comment, and exits 0 green.** A comment read that is truncated, rate-limited or errors is an unreadable issue and the escalation is refused rather than duplicated (D13). **At most one displaced-label marker is outstanding per issue: an escalation fired on an issue that already carries `status:review-stuck` performs no label write and posts its comment with an empty displaced-label field (`<!-- escalation::<reason-code>:<head-oid> -->`), because there is no `status:*` for it to displace.** The closed set of escalation causes is the intent's: consensus not reached within the round cap; a blocking finding still open at the round cap; `hold` or `blocked` present (recorded, not escalated — see D15); a budget gate tripped; a security-tier finding open at merge time. | Reading (a) — comment plus the existing label — is what AGENTS.md:240 and `docs/SPEC.md`'s `lifecycle.labels` already provision, and reading (b) would be a second reversal nobody asked for. The swap rather than an add is forced by the ladder: two `status:*` labels are corrupted state, and `lifecycle_advance.sh` responds to it by commenting and applying `hold` — so an escalation that *added* the label would halt the item through the corrupted-state path and blame the wrong thing. The displaced label goes in the marker rather than in prose because D10 has to restore it deterministically, and the resume protocol already refuses on `status:review-stuck` ("humans have taken over"), so the rung it displaced would otherwise be lost. `blocking-open-at-cap` is REVIEW.md's own round-3 branch adopted rather than reinvented (REVIEW.md:151-153): a pull request whose only open row is an agreed `high` is consensus-*agreed* and would otherwise be a resting state with no exit. The empty displaced-label field on a second escalation is forced by D10's restore: a marker naming `status:review-stuck` as the label it displaced would restore the issue to being stuck. One script rather than one rule two scripts implement, because "the merge gate is the sole writer of escalations" was a claim about a *caller*, and callers multiply — after D14 there are two. Idempotency on `(reason-code, head-oid)` is the general defence against a duplicate escalation and is why no "already escalated" bookkeeping is needed anywhere else in this design: the marker *is* the record that the escalation happened, it already carries both fields, and any caller re-evaluating the same state therefore writes nothing. Round 2 left this unstated and made it acute by routing D14's refusals through a queue — "drains every refusal row not yet escalated" names a predicate the loop ledger cannot answer, since it is append-only, carries no drained field, and D13's `(rung, head-oid)` key belongs to a dispatch row rather than to a refusal row, so every later push would have posted another copy of the one comment a human needed to read (Argus R2-2). The queue is gone (D14); the marker is the join. |
| D10 | **Escalations that a new head can resolve clear themselves; the budget escalation does not.** On each evaluation — the merge-gate runs D3 enumerates, and only those — the merge gate reads **every** escalation marker on the issue in comment order and calls a marker *live* when its condition still holds at that evaluation — for a `consensus-timeout` or `dispute-at-cap` marker, live means the condition has not been resolved by a full consensus at a new head with an empty blocking set or by the dispute's withdrawal **[r2: a `behind` marker is live while the head's `mergeStateStatus` is `BEHIND` and clears at an evaluation where it is not — D24]**. If any marker is live, nothing is restored, nothing merges, and no new marker is written. If none is live, the label restored is the one named by the **earliest** marker carrying a non-empty displaced-label field, `status:review-stuck` is removed in that same `gh issue edit`, and the gate proceeds. `reason-code: budget` and `reason-code: non-monotonic` clear **only** when a human changes the value in `config/execution.yaml` or removes the label by hand; no automatic evaluation clears them. `hold`/`blocked` are not escalations and clear with the label. | The two readings the intent names differ on an item the human answers in a comment and then ignores: self-healing resumes it, by-hand-only leaves it stuck forever — the same "every resting state needs a reachable exit" trap one rung out (REVIEW.md:352-355), and REVIEW.md:355-360 already prefers self-healing to cleanup for exactly this reason. The budget carve-out is not an exception to that principle but an application of it in the other direction: a spend gate that clears itself when the next evaluation happens is a spend gate that fails open, which is item two of the closed `high` list (REVIEW.md:105-106). A ceiling a human must raise is the only kind that is a ceiling. Reading every marker rather than the last is what makes a second escalation well-defined: "restore the displaced label" names no single label once two markers exist, and one still-live `budget` marker must keep the issue stuck however many later `consensus-timeout` markers have cleared. Earliest rather than latest, because only the first escalation displaced a real rung — every later one fired while the issue already stood at `status:review-stuck` and displaced nothing (D9). Naming the evaluations by their triggers rather than leaving "each evaluation" to the reader is what makes the self-healing testable, and it is why D3 enumerates them. |
| D11 | **An escalation stops exactly one issue's ladder and nothing else. It is not repository-wide, it does not stop other issues, and it does not propagate to items that depend on the escalated one.** A dependent item that must not proceed is stopped by a human applying `blocked` to it — the existing act, the existing label. | Per-item is obviously right for a repository running several items and a global stop is obviously wrong; the only genuinely open case was the dependent item, and propagation is rejected because it requires the merge gate to hold a dependency graph. This repository states dependencies in prose inside intent documents (this one's Dependencies section included); a script inferring a stop from prose would be a parser deciding which work halts. `blocked` already exists, is already a refusal in the resume protocol, and is already a human's act. |
| D12 | **A human push to a pull request's head branch is an ordinary head change with no special standing: the recorded consensus stops covering the head (D5 conjunct 3), the reviewers re-review under the funnel, and the system merges when they agree again. REVIEW.md's merge-anytime escape hatch survives unchanged and is the override.** A human push consumes a review round from the per-pull-request cap (REVIEW.md, unchanged) and does **not** consume the per-issue rung-dispatch ceiling, which counts rung dispatches and not rounds (D13). | Reading (b) — a human edit ends the automation for that pull request — reintroduces the bottleneck through the back door every time the operator touches anything, which is the thing this issue exists to remove. The cost objection to (a) is the typo fix that pays for two model runs, and it is bounded by the funnel rather than by a policy: rounds 2 and 3 are verification-scoped, and the operator who does not want to pay even that merges the pull request themselves — "A human can merge or close any PR at any time, at any label state" (REVIEW.md:364), with the recorder filing one follow-up issue for whatever was open. Confirming that hatch survives is load-bearing: with `hold`, it is one of only two ways to override a stuck loop. |
| D13 | **An issue carries two bounds, both evaluated outside any single run, both fail-closed: a rung-dispatch ceiling and a spend ceiling.** They live in `config/execution.yaml` under the `loop:` block D20 adds, as `max_rung_dispatches_per_issue` (shipped default **12**) and `max_cost_usd_per_issue` (shipped default set by the operator in that file, with no in-repository literal duplicating it — #25, D8), and every reader reaches them through `python3 scripts/ops/execution.py --loop <key>` and never by parsing the YAML (D20; #25, D2). The counter is a **loop ledger**: exactly one comment per issue, marked `<!-- loop-ledger:<issue> -->`, one row per rung dispatch carrying the rung, the dispatching event, the pull request, the merged head OID, the timestamp and the run's recorded cost — plus exactly two kinds of row that are **not** dispatches and count against neither bound: one **terminal row** per issue, written at the review transition (D17), and the **refusal rows** a D13 or D14 guard writes, which are a *record* of a refusal and never a work queue for a later run to drain (D14). A dispatch row is idempotent by `(rung, head-oid)`; a refusal row is idempotent by `(rung, head-oid, reason-code)`. It is written by the same deterministic steps that dispatch and merge, and is the only counter — no run counts its own dispatches. **The step that locates the ledger paginates the issue's comments to exhaustive depth — every page, until the API reports no further page — before it may conclude the ledger is absent. A comment read that is truncated, rate-limited, or errors on any page is an *unreadable* ledger, never an absent one.** **Tripping either bound is a green exit** — no dispatch, no merge, one refusal row, and one D9 escalation with `reason-code: budget`, written in the same run by the component that refused (#25, D8; D9, D14). A ledger that cannot be read or parsed is a refusal to dispatch and to merge, not a zero. | The intent makes both mandatory in substance and leaves the shape here: both are specified because they fail differently — a cheap tight loop trips the count long before the dollars, and one expensive rung trips the dollars long before the count, and REVIEW.md:196-198 asks for a budget on the outermost loop, not for one metric. Twelve is five rungs plus headroom for the re-dispatches a healthy item actually takes (a rung whose pull request is closed and reopened, a placement retry), and it is small enough that the runaway in the intent's Problem section — advance, dispatch, merge, misidentify, repeat — trips within one working session rather than one weekend. The ledger is a comment because the counter must survive every run, be readable by a human and by the board (#68/#69) with no new store, and because the recorder's ledger already proves the shape works. Exhaustive pagination is specified rather than assumed because the API's default page is 30 comments and a working issue passes that inside one rung: a truncated first page that missed the ledger would read as "no ledger", reset both bounds to zero, and fail open on the exact runaway the bounds exist to stop — item two of the closed `high` list (REVIEW.md:105-106). The refusal row gets its own idempotency key because a re-run of the workflow that detected the violation re-detects it, and a record that grows one row per re-run is a record nobody can count. Green here and red for D14 is not an inconsistency but the difference between the two causes: a bound reached is the design working exactly as commissioned (#25 D8's cap-exit convention), while a non-monotonic transition is a defect in the label machinery, which #57 D9 already settled ends the run red. |
| D14 | **Monotonic progress is a ratchet on the loop ledger, not on the label: for each issue the ledger records the highest rung index merged, and a dispatch or a merge at an index **strictly less than** it is refused, as is one at an index equal to it whose `(rung, head-oid)` pair the ledger does not already carry. A `(rung, head-oid)` pair the ledger *does* already carry is a no-op, not a violation — the same idempotency key D13 gives the row — so a benign re-run of the workflow over the same merge commit re-detects the already-recorded rung, writes nothing, escalates nothing, and exits 0 green.** Rung index is the row's position in `personas/lifecycle.json` (the same ordering `lifecycle_advance.sh` already uses to rank competing transitions). **The ratchet guards the *label write itself*, not merely the dispatch that follows it, and it is evaluated on every advancer invocation whatever `loop.autonomous_merge` says (D18).** A violation the merge gate detects (D5 conjunct 10) is a refusal to merge plus one escalation through `escalate.sh` (D9). A violation the *advancer* detects — #72's shape, where a merge performed against issue A is misidentified against issue B, which conjunct (10) never examined — resolves in **one run and one ordering**: `lifecycle_advance.sh` writes no `status:*` label, dispatches nothing, appends exactly one loop-ledger **refusal row** on issue B carrying `reason-code: non-monotonic`, the rung, the head OID and the misidentified pull request, and reports the refusal to its caller; then, **in that same workflow run, after the row is written and before the job ends, `.github/workflows/lifecycle.yml` invokes `scripts/ci/escalate.sh` for that refusal, and the job ends red.** `lifecycle_advance.sh` itself writes no `status:*` label and posts no comment on this path. **There is no queue, no drain mode, and no second trigger anywhere in this path.** The ratchet is per issue, so a defect repair that re-enters at PLAN as a **new** issue (#57 spec, D18(b)) is unaffected. | This is the invariant that turns #72's failure scenario from a cycle into a single loud stop: a mis-written stage label sends the ladder backwards, and every per-pull-request and per-run cap is satisfied on every iteration, so nothing else in the system notices. The label cannot be the ratchet because the label is exactly what the defect corrupts; the ledger can, because it is append-only and keyed to merged head OIDs. Escalating rather than silently ignoring is the intent's rule for every bound ("tripping it stops the issue and escalates rather than failing silently"), and it is also the only way the operator learns the advancer is wrong. The re-run carve-out is owed because the ratchet's subject is *progress*, and re-observing recorded progress is not regress: without it, "re-run all jobs" on a healthy merged rung trips `N <= N`, and D10 gives `non-monotonic` no self-clearing path, so one button costs a human a manual label repair on a correct issue. **Round 2's mechanism is withdrawn in full.** It had the advancer park a refusal row and a new `push: main` trigger on `merge-gate.yml` drain it, and both reviewers independently found it defective for the same two reasons. The writer (`lifecycle.yml`) and the drainer (`merge-gate.yml`) fired on the *same* `push: main` event with no ordering guarantee between two workflows, so the drain could read issue B's ledger before the row existed — and a stopped issue emits no further pushes by construction, so the only remaining drain opportunity was an unrelated issue's push, which may never come: D14's own rationale that "the push trigger is what keeps the refusal row from being a resting state with no exit" was falsified by the event ordering it relied on (Atlas AT-4, Argus R2-1). And "every refusal row not yet escalated" was not a decidable predicate: the ledger is append-only with no drained field, D13's `(rung, head-oid)` key belonged to a dispatch row, and D9's marker carried nothing to join a refusal row against — so one drained row would have been re-drained on every later unrelated push, burying the one comment a human needed under N copies (Argus R2-2). Escalating in the same run is what both reviewers named first and is the argument D16 already makes for dispatch: a hand-off to a later event is a hand-off to an event that may never happen. **The cost is stated here rather than lawyered.** #57 spec D8 says *"The advancer … never writes `review:1..3` or `status:review-stuck`"*, and after this row `.github/workflows/lifecycle.yml` does — in a separate step, through the one escalation writer, and only on a refusal to advance. That rule is **narrowed to the script**, and the narrowing is recorded here rather than left to be found in a diff. What #57 D8 protects survives whole, and is why the narrowing is safe: `lifecycle_advance.sh` still forms no opinion about review state — no rounds, no consensus, no `review:N` — and `status:review-stuck` still has exactly one writer (`escalate.sh`), one format (D9) and one idempotency rule. What the advancer contributes is the one fact only it can observe: that this transition would send the ladder backwards. Leaving #57 D8 untouched is what round 2 attempted, and the price was a stopped ladder nobody is told about — a worse outcome for #57's own issue than the narrowing. Red *as well as* escalated, because #57 D9 already settled that a merge-rung integrity failure ends the run red: the escalation tells the issue's reader, the red X tells whoever pushed, and this failure has both audiences. |
| D15 | **`hold` and `blocked` interrupt by being re-read immediately before every write — the merge, the loop-ledger row, the escalation (D9) and the next-rung dispatch — and never by cancelling an in-flight run (#25, D5/D13). Present on the pull request or on any issue it closes (#25, D14) means: discard the computed decision, write nothing at all, exit 0 green, and do not escalate.** A `hold` therefore stops the loop at the next write boundary at the latest, and a `hold` applied while a review run is in flight costs that run and no more. **This binds `escalate.sh` like every other writer, on both of its call paths (D9): a D14 refusal on a held issue records nothing and escalates nothing, and its job ends green rather than red.** | The intent states this as a property and the mechanism already exists: this is #25's dispatch gate applied to one more write, plus trusted-posting rule 5, plus `lifecycle_advance.sh`'s own ordering, which checks `hold` before anything else. Green rather than red because a `hold` is the operator using the system correctly; silent rather than escalating because escalation is how the system asks the human a question, and a `hold` is the human having already answered one. That is also why the D14 refusal path goes green under `hold` rather than red: the red X is the second audience of an escalation, and a held issue has no escalation to have an audience for. This is the conjunct reading (b) of Open question 2 could not satisfy, and the reason D3 chose a script. |
| D16 | **The ladder dispatches its own next rung from inside the transition that writes the label: `lifecycle_advance.sh`, immediately after a successful `status:*` write and its comment, invokes the D17 placement adapter for the new stage's owner with the issue number and nothing else (#25, D16). No second workflow, no `issues: labeled` trigger, no new event.** The adapter is resolved, never guessed: the advancer reads the owner's binding with `python3 scripts/ops/execution.py --binding <persona>` — the ONE parser, never a second read of the YAML (#25, D2) — and invokes `scripts/placement/<placement>/run.sh`. **A persona whose binding's `trigger` is not `ladder` is never dispatched by the advancer**, and `athena`, `daedalus` and `odyssey` move from `trigger: manual` to `trigger: ladder` in `config/execution.yaml` — one line each — where `ladder` is the value D20 adds for exactly this dispatcher. A row whose `advances_to` is null dispatches nothing (D17). **The guards fall in two sets and the sets are not the same: D13, D14 and D15 gate the *label write* and are evaluated whatever `loop.autonomous_merge` says; D18 and a re-read of D15 gate the *dispatch* and are evaluated immediately before the adapter is invoked.** A refusal under D13 or D14 records a loop-ledger refusal row and escalates in the same run via `escalate.sh`, never from a later one (D14). **[r5 (D32, D33): lifecycle_advance.sh passes "$issue" --as "$persona" to placement adapters, superseding "with the issue number and nothing else"; for vm-local placements, scripts/placement/vm-local/run.sh on GitHub Actions executes record-and-return and the external VM poller consumes the dispatch row.]** | Same-run dispatch, rather than a workflow listening for the label, is forced by the same recursion fact D4 rests on: `lifecycle.yml` writes labels with `GITHUB_TOKEN`, and an `issues: labeled` event it generated would start no workflow — the ladder would advance and dispatch nothing, which is the stall moved rather than removed. It also keeps one writer and one ordering: the label, the comment and the dispatch are the same transition, so an item can never be dispatched for a rung whose label was not written. Round 2 flipped the three personas to `trigger: repo-event`, which was wrong twice over: `scripts/ops/execution.py` requires a non-empty `events:` list for that value *and* requires `.github/workflows/unattended.yml`'s `on:` block to list every event so subscribed, so the change was never the three lines acceptance 17 claimed — and `repo-event` *means* "unattended.yml wakes this persona on that event", which is precisely the second workflow this row exists to avoid (Argus R2-6). `ladder` states what is true and is enforceable rather than decorative: it carries no events, it adds nothing to `unattended.yml`, `--subscribers` never returns it, and the advancer refuses to dispatch a persona whose binding does not carry it — so the config value is the gate on who may be dispatched this way. Splitting the guard sets is D18's business and is stated in both rows because round 2 stated the order in three incompatible ways (Argus R2-4). |
| D17 | **The review rung is terminal and owes no dispatch: review happens on the open pull request, before the merge, so `status:in-review` after a system merge is a state with no work owed.** The loop ledger's last row for the issue is written at that transition and marked terminal. Closing the issue remains the human's act and remains #57's Open question 2; nothing here closes, reopens or relabels a finished item (#57 spec, D8). `personas/lifecycle.json` is unchanged by this issue. | Under human merges the review rung was work: a human merged the code, then reviewers read it. Under this loop the reviewers' verdict is what *caused* the merge, so dispatching a review after the merge would pay two model runs to re-read a diff already reviewed at that exact head. The label still gets written, because it is the record that the implement rung completed and it is what the board reads. The per-issue bound stops accruing there by construction, since `advances_to` is null and no further dispatch is possible — which is the intent's worry about an issue that is never closed, answered without touching #57's decision. |
| D18 | **Autonomy is one config value, and it disarms the two *unattended acts* — the merge and the next-rung dispatch — and nothing else. `loop.autonomous_merge: false` in `config/execution.yaml` never disarms a stop.** `scripts/ci/merge_gate.sh` evaluates D5 and writes the loop-ledger row but performs no merge, no escalation and no D10 restore: with the flag false its only write anywhere is that ledger row, and it writes no label and posts no comment beside it. `scripts/ci/lifecycle_advance.sh` writes its `status:*` label and its comment exactly as it does today but **skips D16's dispatch step entirely — including when the push to `main` came from a human merge, which is the only way `main` moves while the flag is false.** **D13's bounds and D14's ratchet are not under the flag.** They gate the *label write*, are evaluated on every advancer invocation whatever the flag says, and their refusal — no label, no dispatch, one refusal row, one `escalate.sh` escalation in the same run, and the exit colour its cause calls for (green for a D13 bound, red for a D14 violation) — is identical in both settings. Each of the two readers reads the value itself, through `python3 scripts/ops/execution.py --loop autonomous_merge` (D20), and reads it fail-closed: unset, unparseable, an unrecognised value, a non-zero exit from the parser, or a missing `config/execution.yaml` means `false` in each. | The intent's reversibility constraint asks that the ladder can still be run with human merges, and the workshop demonstrates both. A boolean read by the one deterministic step is the whole mechanism, and defaulting an unreadable config to "do not merge" is the same fail-closed rule as D5 — the failure that matters is merging when the operator meant not to, never the reverse. It also makes the change safe to land before its dependencies are all green: merged with the flag false, the gate is a recorder of merge-eligibility and nothing more. Naming both readers is load-bearing because D16 puts the dispatch inside the advancer, which runs on **every** push to `main` whatever performed the merge: a flag that disarmed only the merge gate would leave a human's own merge triggering a paid, unattended persona dispatch for the next rung — precisely the unattended spend the flag exists to withhold, and with no way to stop it short of reverting the change. Two independent fail-closed reads rather than one read passed between them, because a guard that travels in from another process is a guard that is absent whenever that process did not run, which is exactly the human-merge case. **Carving the guards out from under the flag is forced by what round 2 wrote.** Its short-circuit ran *before* D14, so with the flag false — the recommended default, and also what an absent or unreadable config produces — the #72 shape would have written the backwards label with no refusal row and no escalation: the exact corruption D14 exists to stop, in the exact configuration this repository runs first (Argus R2-4). The principle the carve-out states is general and worth one sentence: a flag may withhold unattended spend and unattended merges; a guard a flag can switch off is not a guard. The asymmetry between the two components is deliberate rather than untidy — with the flag false the merge gate has merged nothing and a human is doing the merging while reading the same state, so it has no news to deliver; the advancer's ratchet has caught the label machinery itself going backwards, which happens on the human-merge path too and is nobody's ambient knowledge. |
| D19 | **Scope boundary.** The implementing change may touch: `scripts/ci/merge_gate.sh`, `scripts/ci/escalate.sh`, `scripts/ci/lifecycle_advance.sh` (D16's dispatch step; the D13/D14/D15 guards that precede the **label write** and the D18/D15 guards that precede the **dispatch**; and the loop-ledger refusal row a D13 or D14 guard writes — and nothing else: it gains no `status:*` write on a refusal, posts no comment there, and calls no escalation itself), `.github/workflows/merge-gate.yml`, `.github/workflows/lifecycle.yml` (its `permissions:` block, the runtime setup the D20 config read needs, and one step that invokes `scripts/ci/escalate.sh` on a refusal the advancer reported — D14), `scripts/ci/tests/**`, `config/execution.yaml`, `scripts/ops/execution.py` and `scripts/ops/tests/execution_test.sh` (D20), `scripts/auth/app_manifests.yaml`, `docs/SPEC.md`, and the four false-text repairs — AGENTS.md (`:43`, `:168`, `:239-245`), INTENT.md (the human-merge constraint and the per-stage `GATE` lines), README.md (`:102`, `:112`) and REVIEW.md ("Merge is the escape hatch", the comment-only line, and the Labels section's silence about CI). It may **not** touch `personas/**` (D4, D17; #25 D10), `personas/lifecycle.json`, `intent/25-execution-model/spec.md` (D20), `scripts/ops/work.sh`, or any compiled target under `.claude/agents/` or `.agents/agents/`. **This spec's own pull request touches `intent/64-autonomous-loop/spec.md` and nothing else.** The `loop.autonomous` "Agreed, not yet built" bullet that AGENTS.md's living-spec rule would otherwise call for is *not* written here: `docs/**` lies outside Athena's `intent/**` path authority, and a bound a persona may cross by declaring the crossing is not a bound. The entry is owed by the first pull request in this issue's chain whose author's authority admits `docs/SPEC.md` — the implementing one, which under AGENTS.md writes it straight into the spec body rather than into the holding area (acceptance 22). **[r2 (D23, D24): `scripts/ci/lifecycle_advance.sh` additionally admits the loop-ledger dispatch rows and terminal row D13 has the advancer write — omitted above — and the plumbing that writes every ledger row under `MERGE_ACTOR_TOKEN`; `.github/workflows/lifecycle.yml` additionally admits `environment: merge-actor`, the token-mint step and the header-comment repair; `scripts/ci/merge_gate.sh` admits the D24 evaluation of conjunct (2) and the `behind` escalation; `scripts/ci/escalate.sh` admits the seventh reason-code. Nothing else is added.]** **[r5 (D34): D19's file list and prohibitions bound solely issue #64's implementing pull request (PR #257) and impose no prohibition on subsequent issues (such as #251 modifying scripts/ops/work.sh under #251 D8).]** | The four repairs are owed because this change makes their text false, which is a plainer reason than the living-spec rule and the one Atlas's AT-1 on PR #94 established; only the `docs/SPEC.md` edit is owed under that rule, which AGENTS.md scopes to that file alone. `personas/**` is excluded because the merge actor is infrastructure and the trigger flips are `config/` facts, so a persona source that gained an execution field would fail the vendor-agnosticism rule it was written under (#25, D10). The two additions this round are the two D20 needs — the parser it amends and that parser's own test — and the two `lifecycle.yml` clauses D14 needs, because "the `permissions:` block only" would have made the escalation step this round adds a boundary violation and left the implementer choosing between a defective design and an out-of-scope diff. Naming the guard sets separately rather than as "the guards that precede the dispatch" is the same R2-4 repair D16 and D18 carry: two of the three guards precede the *label*, and a scope line that named only the dispatch would have licensed exactly the code round 2 specified. `intent/25-execution-model/spec.md` is on the may-not list because D20 supersedes one clause of it and records the supersession in the artifacts a reader meets, and rewriting a merged spec in a later issue's pull request is how a decision record stops being one. The `docs/SPEC.md` bullet this pull request originally carried is withdrawn on Atlas AT-1 (round 1). The living-spec rule does ask for the record, and this spec is an explicit material decision on the record — but Athena's authority block says in terms that an action exceeding its path bound "is a stop-and-hand-back, not a judgment call", and pre-declaring the crossing in the spec body and in the pull-request body does not convert it into one. Nothing is lost: `spec_check.sh` requires no `docs/SPEC.md` touch for a diff confined to `intent/**`, the decision is on the record in this file, and acceptance 22 already obliges the implementing pull request to carry the `loop.autonomous` entry in the spec body. |
| D20 | **This design owes `config/execution.yaml` exactly two schema additions, and it amends #25 openly rather than smuggling them past #25's own gate: a `loop:` top-level key, and `ladder` as a fourth `trigger` value. `scripts/ops/execution.py` stays the ONE parser — #25's D2 is unamended in substance, and only its schema clause ("only 'personas' is read") is superseded.** `loop:` is **required** and carries a closed key set — `autonomous_merge` (a boolean, shipped **false**), `max_rung_dispatches_per_issue` (a positive integer, shipped **12**) and `max_cost_usd_per_issue` (a positive number the operator sets) — and a missing key, an unknown key or a wrong type is a `--check` failure naming the key, exactly as an unknown key in a persona binding already is. `execution.py` gains one accessor, `--loop <key>`, which prints one value and nothing else; `--check` validates the block, admits `ladder` as a trigger, and keeps requiring `events:` for `repo-event` and only for `repo-event`, so a `ladder` binding adds nothing to `.github/workflows/unattended.yml`. **Every reader of the flag and of the bounds shells out to `execution.py`; no shell script and no workflow parses the YAML a second time (#25, D2), and a non-zero exit or an unrecognised value is fail-closed at the caller — `false` for the flag (D18), a refusal to dispatch and to merge for a bound (D13).** The supersession is recorded where a reader will meet it: in `docs/SPEC.md` under the living-spec rule (acceptance 22), in `config/execution.yaml`'s header comment, and in `execution.py`'s docstring. `intent/25-execution-model/spec.md` is not edited (D19). | Round 2 put the loop bounds under a new top-level `loop:` key and had "each of the two readers read the value itself" — against a parser that has been on `main` since #25 and rejects any top-level key but `personas:` (`scripts/ops/execution.py`: `fail(...has unknown top-level key(s) ...; only 'personas' is read)`), and against #25 D2's single-reader rule this spec's Dependencies table said it consumed unchanged. The implementing pull request would have turned the `execution` job of `ci-gates.yml` red, made D5 conjunct (2) false, and left the merge gate unable to merge its own implementing pull request: the loop could not ship itself (Argus R2-5). Of the two exits, a separate config file was rejected because it does not avoid the cost it was chosen to avoid — two bash readers of a new YAML file is precisely the second schema #25 D2 exists to prevent, and a second parser plus a second CI job is more machinery than one admitted key — while `execution.yaml` already owns this axis in the sense that matters: it is the file where a dollar budget for unattended work is declared, and a per-issue ceiling is that same currency at a wider scope. Amending openly is the only honest option left: a spec that needs another spec's schema to change and does not say so hands the implementer a choice between breaking a gate and inventing policy, and Argus's instruction was to pick now. Making `loop:` required rather than optional is the fail-closed choice at the right layer — an absent block is a red `--check` a human fixes in seconds, where an optional block silently reads `false` and `0` and bricks the loop quietly. Two independent shell-outs rather than one value handed between the readers preserves D18's property — a guard that travels in from another process is absent whenever that process did not run — at the cost of one subprocess per run, while keeping one parser, one schema and one gate over both. |

## Dependencies

Required, not re-specified here. Each row is a thing this design
consumes; none of it is redesigned above. The last column states
where each stands at round 3's base, because three of the seven moved
between round 1 and now (Argus R2-7).

| Issue | What #64 consumes | Whether it still blocks |
|-------|-------------------|-------------------------|
| #8, #9 | The recorder and the findings ledger: the `Reviewed-head` marker, per-row severity and status, the peer-verdict column, the pending timestamp, and the derived consensus axis. D5's conjuncts (3), (4), (5) and (11) read exactly those fields. | Open, and still true at round 3's base: "No recorder exists in this repository yet" (REVIEW.md:19-25). With no ledger there is no recorded consensus, and D5 fails closed on every pull request. **r2:** the recorder's ledger is state for this gate only when the merge actor authored it (D23), so the recorder's writer identity is a constraint #8/#9 inherits. |
| #25 | `config/execution.yaml` and its single parser `scripts/ops/execution.py` (D13, D16, D18, D20), the placement adapter registry `scripts/placement/<name>/run.sh` (D16; #25 D17), the `hold` re-read rule (D15; #25 D5/D13/D14), the cap-exit convention (#25 D8), and the one-line dispatch unit (#25 D16). | **No longer blocks on existence.** The file, the parser and both adapters (`gh-actions`, `vm-local`) are on `main`, and the `execution` job of `ci-gates.yml` already gates them; round 2's claim that "the file does not exist on `main`" is withdrawn (Argus R2-7). What remains is not a dependency but an obligation this issue owns: D20's `loop:` block and `ladder` trigger, added to that file and that parser by this issue's implementing pull request, under that same gate. |
| #72 | The corrected merge discovery, including **D15** (which merged pull request is *the* implementing one), **D16** (the fork gate), **D17** (near miss annotated) and **D18** (the defect-repair path never produces a candidate) of the #57 spec, as amended by **PR #96**. D14's ratchet composes with #72's D15 rather than replacing it: D15 decides *whether* a merge is the implementing one, D14 decides whether that rung may advance at all. | **Delivered.** The issue is closed and the fix is on `main` — `scripts/ci/lifecycle_advance.sh` carries D15's identity test and D16's fork gate. Round 2's "Open `high`" is withdrawn. The intent's runaway scenario was #72 plus self-dispatch; D16 here supplies the self-dispatch, so D14's ratchet remains owed whether or not #72 recurs. |
| #73 | The advancer's guard chain not failing open. | **Delivered.** The issue is closed and the fix is on `main`. Round 2's "Open `high`" is withdrawn. D16 dispatches from inside the advancer, so an advancer that failed open would dispatch on a wrong transition. |
| #74 | The test and doc gaps behind #72/#73. | Open. **Required, and this is the sequencing call the intent left open**: an unattended loop that walks the advancer every rung with nobody reading the result cannot take the fixes on trust. It ships before the flag in D18 is set true; it need not ship before the merge gate lands with the flag false. |
| #57 | The ladder reaching `status:in-review` (delivered, PR #67), the `advances_on` column (D16 reads it), and D8's terminal review rung (D17 preserves it). | Delivered. |
| #68, #69 | The board renders the loop ledger's two new facts: which reviewer verdicts landed at the current head, and where the item stands against its per-issue bounds (D13). | Related, not blocking. The ledger is a comment and is readable without the board. |

## Acceptance

Each item is observable and cites the Decisions it derives from.

1. `DRY_RUN=1 scripts/ci/merge_gate.sh <pr>` prints every mutation and executes none, makes no model call — `scripts/ci/tests/merge_gate_test.sh` puts `claude`, `gemini`, `agy` and `curl` on `PATH` as stubs that fail the run if invoked, and every scenario passes with none of them called — and exits 0 on a pull request it declines (D3).
2. A hermetic scenario with a stub `gh` in which all eleven conjuncts hold prints exactly one merge call, one loop-ledger row, and no label write other than the ladder's; flipping **each** conjunct false in turn produces no merge call and a distinct declined reason, eleven scenarios (D5).
3. For each conjunct, a scenario in which the fact cannot be *evaluated* — the check API errors, the ledger comment is missing, the loop-ledger comment is unparseable, `config/execution.yaml` is absent, `python3 scripts/ops/execution.py --loop <key>` exits non-zero — produces no merge and a non-zero-or-escalating outcome, never a merge and never a silent pass (D5, D13, D18, D20).
4. A pull request whose head is on a fork produces no merge and is logged by number; the fork check runs before the ledger is read (D5 conjunct 1).
5. A pull request whose two reviewer verdicts carry `Reviewed-head` equal to a *previous* OID produces no merge; a push that changes the head between the evaluation and the write is re-read at the write and produces no merge (D5 conjunct 3, D12).
6. A `spec.md` pull request with `Status: Draft`, or with a non-empty `Open questions` line, produces no merge even with both reviewers clean and CI green; a `plan.md` pull request missing `intent/<n>-<slug>/plan.md` likewise. An **implement**-rung pull request, whose `personas/lifecycle.json` row carries `artifact: null`, merges on the strength of the other conjuncts: the artifact-existence fact is TRUE vacuously, is not a false conjunct, and does not reach D5's fail-closed rule for an unevaluable one — the same holds for the `review` row (D2, D5 conjunct 9).
7. An `intent.md` pull request and a code pull request with identical ledger state, identical CI state and identical labels produce identical merge decisions; no branch of `merge_gate.sh` tests the rung to decide *whether* to require review (D1).
8. One reviewer clean at the head and the peer's row pending past the deadline produces no merge and one escalation with `reason-code: consensus-timeout`; the same state with the peer's verdict present produces the merge. An open `security` row with one AGREE produces no merge. A pull request whose Atlas round-1 verdict was clean and whose later rounds are Argus-only fixes at new heads merges with no further Atlas verdict; and six scenarios, one for each way a carry-forward condition fails, produce no merge and reach the deadline instead — no Atlas round-1 verdict on this pull request at all (i), an open `AT-*` row (ii), a `security` row fixed after Atlas's last verdict (iii), an outstanding dispute or mention naming Atlas (iv), an unconsumed admin-applied `deep-review` label (v), and an unanswered explicit thread request for a deep or full review at this head (v). A missing **Argus** verdict at the current head never carries forward (D5 conjunct 3, D7, D8).
9. A human mention of the missing reviewer resets that row's deadline once; a second mention at the same head OID does not reset it again, and the deadline expires (D8).
10. An escalation performs exactly one `gh issue edit` that removes the current `status:*` and adds `status:review-stuck` in the same call, and posts exactly one comment containing both positions and a marker of the form `<!-- escalation:<displaced-label>:<reason-code>:<head-oid> -->`; the issue never transiently carries two `status:*` labels (D9).
11. After a `consensus-timeout` escalation, a later evaluation with both verdicts at a new head and an empty blocking set restores the displaced label, removes `status:review-stuck`, and merges. After a `budget` escalation, the identical later state performs no restore and no merge until `config/execution.yaml` changes. A second escalation on an issue already carrying `status:review-stuck` performs no label write and posts a marker with an empty displaced-label field; with two markers on the record and one still live, no restore happens; once both conditions have cleared, the label restored is the one named by the **earliest** marker with a non-empty field. And escalation is idempotent: a call to `scripts/ci/escalate.sh` whose `(reason-code, head-oid)` pair already appears in a marker on that issue writes no label, posts no comment and exits 0 green, however many times and from whichever of its two callers it is re-evaluated (D9, D10).
12. Escalating issue A writes nothing to any other issue, and an issue whose intent text names A as a dependency is untouched (D11).
13. `hold` applied to the pull request, to its issue, or to any issue it closes, at any point before the write, produces no merge, no comment, no label write and exit 0; the same for `blocked`. A `hold` that lands after the decision is computed and before the write still stops it (D15).
14. The loop ledger is one comment per issue, marked `<!-- loop-ledger:<issue> -->`; re-running the same dispatch for the same `(rung, head-oid)` adds no second row, and re-running the same refusal for the same `(rung, head-oid, reason-code)` adds no second row either; the thirteenth rung dispatch for one issue with the shipped default of 12 produces no dispatch, one `reason-code: budget` escalation written in that same run, and exit 0 green; the same with the spend ceiling reached. An issue whose loop-ledger comment lies beyond the first page of comments is still found — the scenario stubs a multi-page `gh` response — and a comment read that errors or returns a truncated page produces a refusal to dispatch and to merge, never a count of zero (D13, D9).
15. A transition that would write a rung index **strictly below** the ledger's highest merged index writes no label and dispatches nothing, appends exactly one `reason-code: non-monotonic` refusal row to that issue's loop ledger, and — **in the same workflow run, after the row is written and before the job ends** — performs exactly one D9 escalation through `scripts/ci/escalate.sh`, after which the job ends red. The scenario includes the exact #72 shape: a merged pull request misidentified against an issue already past that rung. In this path `lifecycle_advance.sh` itself writes no `status:*` label and posts no comment (D14, D9; #57 spec D8). The absence of the withdrawn mechanism is asserted too: `grep` of `.github/workflows/merge-gate.yml` finds no `push:` trigger, and `grep` of `scripts/ci/merge_gate.sh` finds no drain mode and no reader of refusal rows. Re-running the whole workflow over that same merge commit writes no second refusal row and performs no second escalation — `escalate.sh` finds its own `(reason-code, head-oid)` marker — and the job ends red again with nothing written. Separately, a re-run over a `(rung, head-oid)` pair the ledger already carries as *merged* writes no label, no ledger row and no escalation, and exits 0 green (D13, D14, D9).
16. A successful transition invokes the placement adapter for the new stage's owner exactly once with the issue number as its only argument; the adapter is the one `python3 scripts/ops/execution.py --binding <persona>` names, and a persona whose binding's `trigger` is not `ladder` is not dispatched at all; `grep` of `lifecycle_advance.sh` and `.github/workflows/*.yml` finds no stage, folder, branch, artifact, prompt or model name passed to a dispatch, and no second parse of `config/execution.yaml`; the review row's transition invokes no adapter (D16, D17, D20; #25 D2/D16). **[r5 (D32): qualified by acceptance 29; the invocation passes "$issue" --as "$persona"]**
17. `config/execution.yaml` shows `trigger: ladder` for `athena`, `daedalus` and `odyssey`; the diff for that change is three lines, because `ladder` carries no `events:` list and needs none; `.github/workflows/unattended.yml` is unchanged and gains no event; `python3 scripts/ops/execution.py --subscribers <event>` names none of the three for any event; and no workflow file contains a literal duplicating `max_rung_dispatches_per_issue` or a dollar cap (D16, D20; #25 D8).
18. With `loop.autonomous_merge: false`, or with the key absent, or with `config/execution.yaml` unreadable: a fully merge-eligible pull request is not merged, the merge gate's only write is the loop-ledger row recording the eligible verdict — no label, no comment beside it, no D10 restore — and a **human** merge of that same pull request runs `lifecycle_advance.sh`, which writes the `status:*` label and its comment and invokes no placement adapter, so no dispatch occurs on either path and neither reader depends on the other having run (D18, D16).
19. `.github/workflows/merge-gate.yml` grants `contents: write`, `pull-requests: write`, `issues: write`, `checks: read` and `statuses: read` and nothing else **[r2: as the union of its per-job `permissions:` blocks under a workflow-level `permissions: {}`; acceptance 26 constrains the `pull_request` job alone (R1-6)]**; the merge actor's entry in `scripts/auth/app_manifests.yaml` grants the same five permissions and nothing else; it references its key only as `MERGE_ACTOR_APP_PRIVATE_KEY`; the workflow contains no `pull_request_target` trigger and no `push` trigger; a hermetic scenario in which the escalation and the loop-ledger write are exercised passes with a stub `gh` that rejects any issue write not covered by a granted scope; and the merge actor appears in `scripts/auth/app_manifests.yaml` and in no file under `personas/` (D3, D4, D9, D13; #25 D7/D10).
20. End to end with the flag true on one throwaway issue: it traverses plan → design → build → implement with no human action, each merge is attributable (the merged head OID equals the `Reviewed-head` in the ledger row that authorised it), the issue ends carrying exactly `status:in-review`, the loop ledger holds four rows plus a terminal row, and `.github/workflows/lifecycle.yml` ran on each system merge (D4, D5, D16, D17).
21. `python3 scripts/sync_agents.py --check`, `python3 scripts/ops/execution.py --check`, `bash scripts/ops/tests/execution_test.sh`, `bash scripts/ci/sanitize_check.sh`, `bash scripts/ci/spec_check.sh origin/main` and `bash scripts/ci/tests/merge_gate_test.sh` all exit 0 on the implementing pull request, with no compiled target and no file under `personas/` in the diff (D19, D20).
22. The implementing pull request — not this one — is what touches `docs/SPEC.md`: it adds a `loop.autonomous` entry to the spec body citing its PR, rewords `lifecycle.labels` in place to say the ladder is driven by system merges and dispatches the next rung, records D20's supersession of #25 D2's "only 'personas' is read" schema clause, and carries the four false-text repairs in the same pull request, after which AGENTS.md, INTENT.md, README.md and REVIEW.md contain no surviving sentence asserting that a human merges every path. This spec's own pull request touches no file outside `intent/**` (D19, D20).
23. The #72 shape with `loop.autonomous_merge` explicitly **false**: the misidentified merge writes no `status:*` label, appends one `reason-code: non-monotonic` refusal row, escalates exactly once in the same run, and ends the job red — observably identical to the flag-true scenario except that no dispatch and no merge were ever attempted. The same scenario with `config/execution.yaml` absent behaves the same way. And with `hold` on the issue, the same scenario writes no row, no label and no comment, and ends green (D14, D18, D15).
24. `python3 scripts/ops/execution.py --check` fails, naming the offending key, on a `config/execution.yaml` whose `loop:` block is missing, is missing one of its three keys, carries an unknown key, or carries `max_rung_dispatches_per_issue: 0`; it passes on the shipped file and on a `ladder` binding that carries no `events:`, and still fails a `repo-event` binding that carries none; `--loop autonomous_merge` prints one value and exits 0; and `grep` finds no second reader of `config/execution.yaml` — no shell script and no workflow parses it (D20; #25 D2).
25. The trusted set is the merge actor alone: `grep` of `merge_gate.sh`, `escalate.sh` and `lifecycle_advance.sh` finds no `github-actions[bot]` literal in any trusted-writer set. A consensus-ledger comment authored by `github-actions[bot]` carrying `Reviewed-head` for both reviewers at the current head and zero rows produces no merge — conjunct (11) finds no trusted ledger — and the identical body authored by the merge actor, with the other conjuncts true, merges. A loop-ledger comment authored by `github-actions[bot]` is ignored and the merge actor's ledger on the same issue is the one read; an escalation marker authored by `github-actions[bot]` does not satisfy `escalate.sh`'s idempotency test, and the real escalation is written. Each ignored comment is logged by id and none is a decline (D23).
26. `.github/workflows/lifecycle.yml`'s `on:` block is `push` to `main` and nothing else, its checkout takes `main`'s ref, its job declares `environment: merge-actor` and mints a token from `MERGE_ACTOR_APP_PRIVATE_KEY` that only the ledger-writing and `escalate.sh` steps receive, and its header comment no longer claims the workflow holds no secret; every loop-ledger write and every `escalate.sh` invocation in that workflow runs under `MERGE_ACTOR_TOKEN`, and the `status:*` label writes and handoff comments run under `github.token`. In `.github/workflows/merge-gate.yml` the job that runs on `pull_request` holds no write permission, sets `DRY_RUN=1`, checks out `main` and mints nothing; the mutating job declares `environment: merge-actor` and is excluded from `pull_request`; and a `grep` of every file under `.github/workflows/` finds `MERGE_ACTOR_APP_PRIVATE_KEY` only inside jobs that declare `environment: merge-actor` (D23, P1). A hermetic scenario passes with a stub `gh` that rejects any ledger or escalation write not carrying the merge-actor token (D23, D3, D4).
27. Conjunct (2): a head whose `mergeStateStatus` is `BLOCKED` with every present check `success` produces no merge and names the merge state; `UNKNOWN` on all four reads (a stub returning `UNKNOWN` throughout, four calls counted) produces no merge, no escalation and a ledger row naming `UNKNOWN`; `UNKNOWN` on the first read and `CLEAN` on the second proceeds to clauses 2 and 3; an unreadable field produces no merge as unevaluable; `UNSTABLE` with a present non-gate check pending or failed produces no merge; `CLEAN` or `UNSTABLE` with every present non-gate check `success` and at least one present is true. A foreign failing check named `merge-gate` on the head with `mergeStateStatus` `UNSTABLE` produces no merge; the one entry whose `checkSuite.workflowRun.databaseId` equals the job's `GITHUB_RUN_ID` is excluded and no second entry is; `grep` of `merge_gate.sh` finds no exclusion keyed on a check-run name. A head whose `mergeStateStatus` is `BEHIND` with every other conjunct true produces no merge and exactly one `reason-code: behind` escalation; re-evaluation at the same head produces no second one; and an evaluation at a head that is not `BEHIND` clears the marker (D9, D10). `grep` of `merge_gate.sh` finds no request under `branches/` and no `protection` path; the merge actor's manifest entry and acceptance 19's five-permission union are unchanged; and one GraphQL read of `mergeStateStatus` with the minted merge-actor token is recorded with acceptance 28's evidence (D24, D4).
28. P1 and P2 are discharged and recorded, each with its evidence command, its date and the identity that ran it, in `docs/SPEC.md`'s Deployment status by the pull request that sets `loop.autonomous_merge: true` — which is also the pull request acceptance 20 runs against; no earlier pull request sets the flag true (D23, D24, D18).
29. A successful transition invokes the placement adapter passing "$issue" --as "$persona"; for vm-local placements under GITHUB_ACTIONS=true, scripts/placement/vm-local/run.sh logs delegation and exits 0; the external VM poller consumes the unconsumed dispatch row on the operator VM; and .github/workflows/lifecycle.yml retains push-to-main trigger and existing permissions (D32, D33, D34).

## Out of scope

- **The recorder, the ledger schema and the review protocol's content** stay with #8/#9 and REVIEW.md. This design consumes recorded fields and adds none; where the derivation has a gap it is closed in the protocol, not in the merge gate (intent, Proposed outcome).
- **Who closes a finished item.** D17 keeps #57's D8: the ladder ends at review and the human closes. #57's Open question 2 stays open and stays #57's.
- **Intake.** Filing the `intent:new` issue remains the human's act (#10, intent, Proposed outcome).
- **The defect-repair path (#32)** is unchanged; D14's ratchet is per issue and a repair that re-enters at PLAN is a new issue (#57 spec, D18).
- **The board's rendering.** #68/#69 own the surface; D13 gives them a ledger to read and specifies no columns for them.
- **Any change to `personas/lifecycle.json` or `personas/**`.** The merge actor is infrastructure (D4) and the trigger flips are `config/` facts (D16).
- **Branch protection settings on `main`.** Whatever protection exists must admit the merge actor and no other agent identity; applying it is a repository setting a human makes, like the required-checks setting in `docs/SPEC.md`'s Deployment status. **r2:** the `merge-actor` GitHub Environment is the same kind of setting — its deployment-branch policy admitting `main` only is what D23 relies on, and both settings are the operator's to set and to discharge as P1 and P2 (acceptance 28).
- **Editing `intent/25-execution-model/spec.md`.** D20 supersedes one clause of #25's D2 and records the supersession in `docs/SPEC.md`, in `config/execution.yaml`'s header comment and in `execution.py`'s docstring; the merged spec stays as the record of what was decided then (D19, D20).
