#!/usr/bin/env bash
# Gate evidence for #6: a behavior-bearing path (scripts/) touched with
# no docs/SPEC.md update and no Spec-impact marker in the PR body. The
# spec check must fail this pull request.
set -euo pipefail
echo "demo"
