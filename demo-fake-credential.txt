Gate evidence for #6 — NOT a real credential.

The line below is an obviously fake GitHub token: the prefix is real,
the body is twenty x characters. It exists so the sanitization gate
has something to catch on a real pull request.

ghp_xxxxxxxxxxxxxxxxxxxx
